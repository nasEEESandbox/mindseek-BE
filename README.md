# mindseek-BE

to build (deps changes), use 
```docker-compose up --build```
<br>to run (code changes), use `docker-compose up`<br>
add `-d` flag for detached mode<br>

to stop, use `docker-compose down`<br>

use `docker-compose logs -f` to see logs
